/* Increases a counter to 5 by using compound operators*/
// Page = 86

#include <stdio.h>
main()
{
    int ctr = 0;

    ctr += 1;  //Increases to 1
    printf ( "Counter is at %d.\n", ctr );

    ctr += 1;  //Increases to 2
    printf ( "Counter is at %d.\n", ctr );

    ctr += 1;  //Increases to 3
    printf ( "Counter is at %d.\n", ctr );

    ctr += 1;  //Increases to 4
    printf ( "Counter is at %d.\n", ctr );

    ctr += 1;  //Increases to 5
    printf ( "Counter is at %d.\n", ctr );

    return 0;

}

